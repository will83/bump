name = "bump-cleanup"
main = "cleanup-cron.js"
compatibility_date = "2024-12-01"

# Cron: toutes les heures
[triggers]
crons = ["0 * * * *"]

# Bindings (même que l'app principale)
[[d1_databases]]
binding = "DB"
database_name = "bump-db"
database_id = "09105e88-8ab2-49fa-a36e-4d4f437b7211"

[[r2_buckets]]
binding = "BUCKET"
bucket_name = "bump-files"
